# GCS-File-Portal
The GCS File Portal is a web application developed using Python and Flask that allows users to upload, manage, and access files stored in Google Cloud Storage (GCS). Instead of saving files on the local machine, the application stores them securely in a Google Cloud Storage bucket, providing scalable and reliable cloud-based file management.
